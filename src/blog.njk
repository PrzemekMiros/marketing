---
layout: layouts/blog.njk
title: Artykuły
description: Porady i ciekawostki dotyczące tworzenia stron, pozycjonowania i digital marketingu. 
pagination: 
    data: collections.posts
    size: 15
    alias: blog
---

<section class="pt-xsmall pb-large" data-scroll-section>
  <div class="container">
      <div class="row gx-4"> 
        {% for post in blog %} 
        <div class="col-md-4 pb-small pb-md-medium"> 
            <a href="{{ post.url }}" title="{{ post.data.title }}">
                        <div class="portfolio-item fade-in">
                                <div class="parallax-wrap aspect_16-10">
                                  {% blogImage "src/" + post.data.thumbnail, post.data.title %}
                                </div>
                        </div>
                        </a>
                        <p class="archive-category my-3 fade-in">
                            <small class="color-accent-1 bold">
                                {{ post.data.tags }}
                            </small>
                        </p>
                        <a href="{{ post.url }}" title="{{ post.data.title }}">
                            <h3 class="h4 split-lines line-clamp-2 mt-0 mb-2">
                                {{ post.data.title }}
                            </h3>
                        </a>
                        <a href="{{ post.url }}" title="Czytaj" class="button magnetic fade-in mt-3 mb-4" data-strength="100" data-strength-text="50">
                            <span>Czytaj</span>
                            <svg width="11" height="11" viewBox="0 0 14 14" fill="none"
                                xmlns="http://www.w3.org/2000/svg">
                                <path d="M13 1L1 13" stroke="var(--color-accent-1)" stroke-width="2"
                                    stroke-linecap="round" stroke-linejoin="round"></path>
                                <path d="M3 1H13V11" stroke="var(--color-accent-1)" stroke-width="2"
                                    stroke-linecap="round" stroke-linejoin="round"></path>
                            </svg>
                        </a>
                        <div class="line-x lx-1 lx-op"></div>
             </div> 
      {% endfor %} 
    </div>
      <div class="row gx-4">

        <div class="col-md-6">
          <nav>
            <ul class="pagination d-flex fade-in"> {%- for pageEntry in pagination.pages %} 
              <li class="pagination-item">
                <a class="pagination-link" title="Paginacja" href="{{ pagination.hrefs[ loop.index0 ] }}" {% if page.url == pagination.hrefs[ loop.index0 ] %} aria-current="active" {% endif %}>{{ loop.index }}</a>
              </li> {%- endfor %} </ul>
          </nav>
        </div>
      </div>
    </section>